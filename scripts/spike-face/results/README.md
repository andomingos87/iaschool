Saídas da execução de 31/08/2026 (Apple M4, 15 núcleos, CPU, onnxruntime 1.29).
Ver a leitura em `docs/spike-reconhecimento-facial.md`.

`accuracy.json` foi gravado sem o campo `raw_rows` — ele contém os nomes das
pessoas do LFW por sonda, e não há motivo para versionar isso.
